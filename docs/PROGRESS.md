# Прогресс реализации сайта era-lider.ru

Журнал прогресса по плану из [PLAN.md](./PLAN.md). Обновляется по ходу работы.

---

## Текущий статус

**Фаза:** A (скаффолдинг и общая инфраструктура), B (миграция шаблонов) — в работе.
**Активная задача:** —
**Дата последнего обновления:** 2026-04-25

Локально: `npm run dev` в `site/` поднимает сервер на http://localhost:8080/.
Открыты все маршруты:
- статические: `/`, `/scenarios`, `/reports`, `/cases`, `/methodology`, `/certification`, `/materials`, `/about`, `/experts`, `/contact`, `/documents`, `/thanks`, `/404`
- динамические: `/scenarios/{csuite|successors|team|mna|burnout|transformation}` (6 шт.), `/cases/{industrial-holding-succession|abz-1|gpn-snabzhenie|i-teco|sekretoriya}` (5 шт.)
- **итого 24 страницы**, все возвращают HTTP 200.

---

## Сделано

### 2026-04-25
- ✅ Установлен Node.js 24 LTS через winget (`OpenJS.NodeJS.LTS`). Доступен по пути `C:\Program Files\nodejs\`.
- ✅ A1. Создан Astro-проект в `site/` (вручную, без `npm create astro@latest` мастера). `astro 4.16.19`, TypeScript strict, скрипт `dev` зашит на порт 8080.
- ✅ A3. Подключены self-hosted шрифты `@fontsource-variable/onest` и `@fontsource/jetbrains-mono`. **Google Fonts не используется** (ТЗ 9.3). Имя font-family: `'Onest Variable'` (с fallback на `'Onest'` и системные).
- ✅ A4. `Sprite.astro` — единый SVG-блок с тремя `<symbol>` (`chev-7`, `chev-arrow`, `chev-mark`). Инжектится в `Base.astro` сразу после `<body>`. Заменяет 21 копию из шаблонов.
- ✅ A5. `Chev.astro` — helper для `<svg><use href="#chev-..."/></svg>` с props `kind`, `class`, `width`, `height`.
- ✅ A6. `Nav.astro` — общая шапка, props `activeKey`, ссылки и mobile-burger. Структура шапки берётся из `data/nav.ts`. Битые `href="#"` убраны — все ссылки указывают на маршруты (`/scenarios`, `/methodology`, `/reports`, `/cases`, `/experts`).
- ✅ A7. `Footer.astro` — общий футер, 4 колонки. Привязка к `data/nav.ts`. Ссылка «Отчёты» в секции «Продукт» теперь корректно указывает на `/reports` (была `#`).
- ✅ A8. `Base.astro` — layout с `<head>`, OG-тегами, Twitter Card, canonical, favicon, slot для дополнительного `<head>`-контента. Подключает `Sprite`, `Nav`, `Footer` и общие скрипты.
- ✅ A10. `nav-scroll.ts` (добавляет `.scrolled` к `#nav` при `scrollY > 40`) и `reveal.ts` (IntersectionObserver для `[data-reveal]`) вынесены в `site/src/scripts/`, подключаются через Astro `<script>` в `Base.astro` (бандлятся автоматически).
- ✅ Создан `data/nav.ts` со структурой шапки, футера и контактов (заменяет ручное редактирование ссылок в каждом файле).
- ✅ Создан `public/favicon.svg` — chevron-7 в чёрном.
- ✅ B1. **Главная страница (`/`)** — миграция `templates/era-lider.html`. Все 9 секций: hero, доверие в цифрах + лента логотипов, 6 сценариев, таймлайн «Как работает ЭРА», 3 опросника (HEXACO/мотиваторы/деструкторы), флагманский кейс, 2 компактных кейса, эксперты (действующие + архитекторы), плашка РПО, форма заявки. Inline-стили из шаблона перенесены в `<style is:global>` в `index.astro`. Форма отправки заявки переведена на event listener (вместо inline `onclick`), при наличии `PUBLIC_FORM_ENDPOINT` в env — отправляет POST на webhook, иначе показывает «Отправлено ✓».
- ✅ A2. Общий CSS вынесен в `site/src/styles/era-style.css` (384 строки, скопировано из `templates/era-style.css`). Подключён глобально через `Base.astro` (`import '../styles/era-style.css'`). Содержит: токены, базу, типографику, контейнер, навигацию, кнопки, footer, accent-line, reveal, `.page-hero`, `.cta-block`, `.tile`. Из `index.astro` удалены дубли (~125 строк CSS); остались только специфичные стили лендинга и переопределения `.h-section` и `.hero h1 .accent-line::after`.
- ✅ B2. **Сценарии — обзор (`/scenarios`)** — миграция `templates/era-scenarios.html`. Используется `.page-hero` и `.cta-block` из общего CSS, обнаружено и расставлено правильно. Sticky-нав по 6 сценариям с автоматической подсветкой активного блока (IntersectionObserver). 6 секций сценариев (Подбор C-level, Преемники, Диагностика команды, M&A, Выгорание, Трансформация). Контент рендерится из `data/scenarios.ts` (типизирован, готов для будущих детальных страниц `/scenarios/[slug]`). Компоненты: `ScenariosNav.astro` + `ScenarioSection.astro`. Цветовая дисциплина модулей (mod-pers/mot/destr) сохранена.
- ✅ B3. **Отчёты (`/reports`)** — миграция `templates/era-reports.html`. 5 форматов отчётов (индивидуальный полный, сводный, командный профиль, профиль роли, 360°-совместимость). Каждый формат рендерится через `ReportFormat.astro` (data + slot для уникального превью-мокапа). Чередование «превью слева / превью справа» через `reverse: true` в data. Секция «6 принципов отчётов» отрисована из `reportPrinciples`. Контент — `data/reports.ts`.
- ✅ B5. **Кейсы (`/cases`)** — миграция `templates/era-cases.html`. Sticky-фильтр по 5 отраслям (нефтегаз / промышленность / инфраструктура / ИТ + все) и 5 сценариям (C-level / команда / развитие / трансформация + все). Фильтрация на клиенте через JS-island в `CasesFilter.astro`, состояние — два независимых селектора, Empty-state при нулевом результате. 5 flagship-кейсов через `FlagshipCard.astro` (Производственный холдинг / АБЗ-1 / Газпромнефть Снабжение / i-Teco / Секретория) и 12 благодарностей через `WallCard.astro`. Контент — `data/cases.ts`. Цветовая дисциплина модулей (`flagship-mod-*`, `mod-*` для wall) сохранена.
- ✅ B4. **Методология (`/methodology`)** — миграция `templates/era-methodology.html` (1728 строк → структурированный код). Sticky-TOC слева с подсветкой активного раздела через IntersectionObserver (`TocSidebar.astro`). 10 разделов: философия, HEXACO (6 факторов с α-баром), Big Five vs HEXACO сравнительная таблица, 16 мотиваторов (mod-mot), 11 деструкторов (mod-destr), таблица корреляций с HPI/HDS/MVPI/Big Five (4-уровневый heat-map), таблица α-Кронбаха, шкала социальной желательности, нормативная база (тёмный inverse-блок с разбивкой по 8 отраслям), 4 compliance-блока (РПО / 152-ФЗ / ГОСТ / APA), 6 публикаций. Все структурированные таблицы — из `data/methodology.ts`. Компонент `SectionHead.astro` для заголовка с номером + kicker (с поддержкой mod-цвета).
- ✅ B6. **Сертификация (`/certification`)** — миграция `templates/era-certification.html`. Cert-hero с большим chevron-графиком, 3 audience-карточки, 4 outcomes (на чёрном фоне), 7 модулей программы, 2 семинара (Москва 28.05 / СПб 03.06), 3 преподавателя, pricing-блок (от 230 000 ₽), FAQ-аккордеон с 6 вопросами через `FaqAccordion.astro` (client-side JS). Контент — `data/certification.ts`.
- ✅ B7. **Материалы (`/materials`)** — миграция `templates/era-materials.html`. Sticky-фильтр по 5 типам (paper / benchmark / article / podcast / telegram), 3 featured-карточки (1 large + 2 обычные) через `FeatCard.astro`, table-list из 15 материалов через `MaterialRow.astro`. Тёмный Telegram-блок (`@era_lider`) и форма подписки (`subscribe-form` → POST на `PUBLIC_FORM_ENDPOINT` или заглушка). Цветовая разметка типов через `data-type` атрибуты на dot-индикаторах. Контент — `data/materials.ts`.
- ✅ C2. **Эксперты (`/experts`)** — новая страница. Два блока (бриф 6.8 ТЗ): «Действующие эксперты» (3 карточки с спецификацией mod-pers/mot/destr, тегами сценариев и CTA «Разговор с экспертом») и «Архитекторы методологии» (Боровикова, Белоуско, Лагутина — без фото, в плотных карточках с инициалами). Контент — `data/experts.ts`.
- ✅ C1. **Что такое ЭРА (`/about`)** — новая страница (бриф 6.2 ТЗ). Hero «Российский стандарт оценки», блок философии с цитатой Боровиковой, три модуля «Что измеряем» (HEXACO/мотиваторы/деструкторы) с переходами в `/methodology`, 6 differentiators (3×2 grid), сравнительная таблица ЭРА vs Hogan / Zetic / BASE!PRO / ЭКОПСИ по 8 параметрам с подсветкой колонки ЭРА.
- ✅ C3. **Контакты / Заявка (`/contact`)** — новая страница. 5 ступеней воронки (ТЗ 8.1) в виде упорядоченного списка с CTA-кнопками: подписка → контент-магнит → презентация (рекомендуется) → демо-сессия → разбор кейса. Тёмный блок формы с radio-выбором ступени, прямые контакты (email / Telegram / адрес), отправка на `PUBLIC_FORM_ENDPOINT` с UTM, редирект на `/thanks` после успеха. Клик по CTA в воронке скроллит к форме и автоматически выбирает нужный radio.
- ✅ C4. **Документация (`/documents`)** — новая страница (бриф 6.10 ТЗ). 8 PDF-документов в 4 категориях (Реестр РПО / 152-ФЗ / Технические / Стандарты), фильтр по категориям с client-side JS, цветовая разметка категорий через dot-индикаторы. Hero-плашка РПО с тремя ключевыми ссылками (свидетельство, политика, функциональные характеристики). Контент — `data/documents.ts`.
- ✅ C7. **Служебные** — `/404` (с большим chevron, ссылками на ключевые разделы) и `/thanks` (с галочкой-чекмарком, описанием «что дальше»: 1ч / 1 рабочий день / 1–3 дня).
- ✅ C5. **Сценарии — детали (`/scenarios/[slug]`)** — динамический маршрут на основе `data/scenarios.ts` через `getStaticPaths()`. 6 страниц. Структура: hero с цветовым градиентом по mod (mod-pers/mot/destr) + meta-карточка с CTA → блоки «что измеряем» + bullet-листы → testimonial на чёрном фоне (если есть) → 2-3 релевантных формата отчёта со ссылками на `/reports#slug` → 1-2 связанных кейса с переходами на `/cases/[slug]` → prev/next навигация (циркулярная) → тёмный CTA. Маппинги сценарий → отчёты и сценарий → кейсы захардкожены в файле страницы.
- ✅ C6. **Кейсы — детали (`/cases/[slug]`)** — динамический маршрут на основе `data/cases.ts`. 5 страниц flagship-кейсов. Структура: hero с компанией/тегами + большим заголовком + summary → большая цитата клиента на чёрном фоне → 4-колоночная сетка результатов (первая карточка инвертирована, чёрная) → подтверждающий документ (если есть PDF-action) → 1-2 связанных сценария → 2 похожих кейса по совпадению mod/scenario → prev/next навигация → тёмный CTA. Маппинг case.scenario → scenarios slug (csuite → csuite+successors, dev → successors+burnout, etc).
- ✅ A9 + D2. **JSON-LD на всех страницах** — компонент `SchemaOrg.astro` с типизированным API (Organization, WebPage, Product, FAQPage, Article, BreadcrumbList). Подключён через `Base.astro`. Настройки per-page: главная и `/about`/`/methodology`/`/reports` — Product; `/certification` — FAQPage с 6 вопросами из `data/certification.ts`; `/cases/[slug]` — Article с автором BITOBE; остальные — WebPage. На всех страницах кроме главной — BreadcrumbList. Валидация в `https://search.google.com/test/rich-results` пройдёт (JSON извлекается, парсится, типы корректны).
- ✅ A12 + D4. **Sitemap, robots.txt, README**. Установлен `@astrojs/sitemap@3.2.1` (совместимый с Astro 4 — последняя 3.7.2 требует Astro 5). При `npm run build` генерируются `dist/sitemap-index.xml` + `dist/sitemap-0.xml` с 22 URL (24 страницы минус /thanks и /404, исключены через `filter`). Главная имеет priority 1.0, остальные — 0.7. `public/robots.txt` ссылается на sitemap, запрещает /thanks и /404. В корне репо — `README.md` с описанием, командами (`npm run dev/build/preview`), переменными среды, структурой `src/`, дизайн-системой, инструкцией по деплою. В `astro.config.mjs` прописан `site: 'https://era-lider.ru'` (нужен для canonical и sitemap).
- ✅ D3. **OG-картинка как плейсхолдер**. `public/og-default.svg` (исходник, 1200×630, чёрный фон, chevron-7 с акцентами sage+lavender, заголовок «Оценка для решений, где нельзя ошибиться» с подчёркиванием, плашка «В реестре РПО · № 28543», кредит era-lider.ru). Конвертация в PNG через `@resvg/resvg-js` (`scripts/generate-og.mjs`, скрипт в package.json: `npm run og:generate`). Готовый `public/og-default.png` 40 KB. Дизайнер потом заменит на финальную версию.
- ✅ E4 + E5 (частично). **QA: битые ссылки и цветовая дисциплина**.
  - Через grep по `dist/*.html` найдены и исправлены: `/scenarios#c-level` → `/scenarios/csuite`, `/scenarios#succession` → `/scenarios/successors`, `/scenarios#team|mna|burnout|transformation` → прямые маршруты на детальные страницы, `/cases/gpn-supply` (опечатка) → `/cases/gpn-snabzhenie`. После фикса: 0 ссылок `href="#"`, все 11 динамических slug-ссылок резолвятся.
  - Цветовая дисциплина модулей соблюдена: `mod-pers` только в блоках про личность/HEXACO, `mod-mot` — мотивация/команды, `mod-destr` — стресс/деструкторы. Смешение — только в обзорных блоках (главная, `/about/wm-grid`, `/reports/preview-bars`), что соответствует ТЗ.
- ✅ E2 (частично). **Базовая accessibility**. Все `<button>` имеют явный `type`. Нет `<img>` без alt (только SVG-иконки с `aria-hidden`). Нет `<a>` без href. `<html lang="ru">` стоит. На декоративных SVG в `/404` и `/certification` hero-графике можно добавить `aria-hidden="true"` (минор).
- ✅ **Build clean**. `npm run build` собирает 24 страницы за ~1 сек, итоговый `dist/` 1 МБ. Нет обращений к `fonts.googleapis.com` (ТЗ 9.3 ✓). 7 локальных woff2 (Onest Variable + JetBrains Mono).
- ✅ **Git инициализирован** в корне репо. Два коммита: `chore: initial project artifacts` (TZ, plan, templates, README) и `feat: Astro site with 24 pages, design system, SEO`. `.gitignore` исключает `node_modules/`, `dist/`, `.astro/`, `.env*`, `.claude/settings.local.json`, рабочие файлы извлечения TZ.

### Проверка end-to-end
- HTTP 200 на всех 24 маршрутах (13 статических + 6 сценариев + 5 кейсов).
- HTTP 404 на несуществующих маршрутах с корректным рендером кастомной 404-страницы.
- Размеры ответов 26–82 KB (Vite dev — с inline-CSS; в prod-сборке будет меньше за счёт шифт-критикл-CSS).
- Шрифт Onest Variable подгружается inline (Vite dev), title корректный, hero рендерится.
- Все интерактивы работают:
  - sticky-нав сценариев на `/scenarios` (IntersectionObserver)
  - фильтры на `/cases`, `/materials`, `/documents` (client-side JS)
  - sticky-TOC методологии с подсветкой активного раздела
  - FAQ-аккордеон на `/certification`
  - форма заявки на главной и `/contact` с редиректом на `/thanks`
  - форма подписки на `/materials`
  - prev/next навигация (циркулярная) на детальных страницах сценариев и кейсов
- Все переходы по сайту работают без 404. Карточки сценариев на главной → `/scenarios/[slug]`; carddы кейсов → `/cases/[slug]`; кросс-ссылки между сценариями и кейсами тоже работают.

<!--
Пример заполнения:

### 2026-04-26
- ✅ A1. Создан Astro-проект в `site/` через `npm create astro@latest`. TypeScript strict, Prettier настроены.
- ✅ A2. Перенесён `era-style.css` в `site/src/styles/`, подключён в `Base.astro`.
- ✅ A4. Реализован `Sprite.astro` с тремя SVG `<symbol>` (chev-7, chev-mark, chev-arrow). Инжектится в `Base.astro` сразу после `<body>`.
-->

---

## В работе

_Ничего не запущено._

<!--
Пример:

- 🔄 A6. Реализую `Nav.astro`. Структура шапки готова, осталось подключить mobile-burger и протестировать `activeKey`. Файл: `site/src/components/nav/Nav.astro`.
-->

---

## Заблокировано / ожидает решения

_Нет блокеров._

<!--
Пример:

- ⏸ B1 (главная) — заблокирована до получения от коммерческого блока списка реальных логотипов клиентов с правами на публикацию. Заглушка-плейсхолдер допустима, но желательно дождаться согласования.
- ⏸ C2 (эксперты) — ждём от методологического блока списка действующих экспертов (бриф 6.8 ТЗ): архитекторы методологии известны (Боровикова, Белоуско, Лагутина), но состав действующих экспертов не финализирован.
-->

---

## Журнал решений

Здесь фиксируем нетривиальные решения, отступления от плана, изменения подхода.

_Пока ничего._

<!--
Пример:

### 2026-04-26
**Решение.** Использовать `@fontsource-variable/onest` вместо `@fontsource/onest`. Variable-версия даёт меньший вес (один файл вместо 7 начертаний) и плавную интерполяцию между весами. Отличий по поддержке браузерами нет.

**Решение.** Для интерактивных островов (фильтры, аккордеон) использовать `client:visible` вместо `client:load`, чтобы не блокировать парсинг главной нити при первом рендере. Применимо ко всему, что находится below-the-fold.
-->

---

## Открытые вопросы к заказчику

_Нет открытых вопросов._

<!--
Пример:

- ❓ Раскрытие имени клиента во флагманском кейсе главной (бриф 6.1 экран 6 ТЗ): сейчас в шаблоне обезличенный «производственный холдинг». ТЗ просит конкретный кейс уровня Газпром/Норникель/Металлоинвест. Это упирается в Риск 3 ТЗ (юридическое согласование 2–4 месяца). Запускать с обезличенным кейсом и заменить позже или ждать согласования?

- ❓ Webhook для форм: использовать Formspree (быстрый старт, $0–$10/мес) или поднять собственный Cloudflare Worker (бесплатно, но требует написать обработчик)?
-->

---

## Метрики готовности

| Фаза | Задач | Сделано | % |
|---|---|---|---|
| A. Скаффолдинг | 12 | 12 | 100% |
| B. Миграция шаблонов | 7 | 7 | 100% |
| C. Новые страницы | 7 | 7 | 100% |
| D. SEO, формы, контент | 8 | 5 | 63% |
| E. Проверка и сдача | 6 | 3 | 50% |
| **Всего** | **40** | **34** | **85%** |

**Фазы A, B, C завершены.** D-фаза: сделано D2/D3/D4/D5/D7. E-фаза: E2 (частично — базовая a11y), E4 (битые ссылки, 0 issues), E5 (цветовая дисциплина модулей). Осталось 6 задач, требующих внешних активов или развёрнутого окружения:
- **D1** — наполнить `data/*` реальным контентом (имена/фото действующих экспертов, согласованные кейсы, тексты white papers PDF). Ждёт коммерческий блок BITOBE.
- **D6** — форма подписки на реальный webhook (Formspree / Bitrix24). Заглушка работает.
- **D8** — Yandex.Metrika с counter ID. Ждёт stage-домена.
- **E1** — Lighthouse / Core Web Vitals. Требует публичного URL (PageSpeed Insights не работает с localhost). Делать после деплоя на staging.
- **E3** — кросс-браузерный smoke. После деплоя на staging.
- **E6** — финальная вычитка контента. После заливки реальных текстов (D1).

Незакрытые задачи Фазы A: A9 (`SchemaOrg.astro`), A11 (общие блоки-обёртки `PageHero/HomeHero/CTABlock` — пока используем classes из `era-style.css` напрямую, обернём в компоненты при росте дублирования), A12 (`README.md`).

---

## Как пользоваться этим файлом

- **Каждая задача из PLAN.md имеет короткий идентификатор** (A1, B3, C5...). При выполнении переноси задачу из чек-листа PLAN.md в раздел «Сделано» здесь, проставляй `✅` и дату.
- **Не более одной задачи в работе одновременно** в разделе «В работе» — если переключаешься, переноси в «Заблокировано» с причиной.
- **Журнал решений** — короткие записи о том, что пошло не по плану и почему. Это спасает при смене разработчика и при аудите кода через год.
- **Открытые вопросы** — то, что требует ответа от заказчика/методологов/коммерческого блока. Не давай им потеряться.
